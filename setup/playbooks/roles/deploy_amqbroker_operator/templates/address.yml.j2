---
apiVersion: broker.amq.io/v2alpha2
kind: ActiveMQArtemisAddress
metadata:
  name: {{ item.name }}-example-address
  namespace: {{ item.name }}
spec:
  addressName: example-address
  queueName: example-queue
  routingType: anycast