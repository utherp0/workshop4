apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: layer2-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - layer2-layers.apps.cluster-c2d5.c2d5.example.opentlc.com